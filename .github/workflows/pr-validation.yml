name: PR Validation

on:
  pull_request:
  push:
    branches: [master, main]

permissions:
  contents: read
  pull-requests: write

jobs:
  validate:
    uses: jasonv2610/jvi-core/.github/workflows/reusable-pr-validation.yml@main
    secrets: inherit
